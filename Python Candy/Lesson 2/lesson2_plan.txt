Lesson 2

variable haqqında məlumat
python user input/ python istifadəçi girişi (input=giriş)
pythondatatypes/numeric(sayısal)
Numeric Data Types
   -integer(tamsayı), 
   -complex number(qarışıq sayılar), 
   -float(kəsir ədədlər)
Sequence Data Type
   -tuple(dəst), 
   -string(sim), 
   -range(aralıq),
   -list, 
   -bytes
   -bytearray
   
   -bytes immutable
   -bytearray mutable
